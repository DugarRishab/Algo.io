#include <bits/stdc++.h>

using namespace std;

class Solution {
public:
    string longestPalindrome(string s) {

		string revS = s;
		vector<int> map(s.length());
		for (int k = 0; k < s.length(); k++){

		}
			// reverse(revS.begin(), revS.end());

		int i, j, front = 0, back = s.length();

		i = 0;
		j = s.length() - 1;

		for (int k = i; k <= j; k++){
			if(s[i] == s[j]){
				
			}
		}
	}
};